# OpenAI Realtime API 服务器配置
# 复制此文件为 .env 并填入你的配置

# ==================== 服务器配置 ====================
DEBUG=true
SERVER_HOST=0.0.0.0
SERVER_PORT=8000

# CORS 跨域配置 (多个来源用逗号分隔)
# 开发环境(DEBUG=true): 留空或仅空格会使用 allow_origins=['*'] (会显示警告；并且会禁用 allow_credentials)
# 生产环境(DEBUG=false): 留空或仅空格会默认使用 ["http://localhost:3000", "http://localhost:8000"]
# 生产环境建议: 指定具体来源，例如 https://yourdomain.com
# 示例: CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ORIGINS=

# ==================== STT 语音转文字配置 ====================
# 可选: deepgram, openai_whisper, local_whisper
STT_PROVIDER=deepgram

# Deepgram 配置 (STT_PROVIDER=deepgram 时需要)
DEEPGRAM_API_KEY=your_deepgram_api_key_here
DEEPGRAM_MODEL=nova-2
DEEPGRAM_LANGUAGE=zh-CN

# OpenAI Whisper 配置 (STT_PROVIDER=openai_whisper 时需要)
# 使用 OPENAI_API_KEY

# 本地 Whisper 配置 (STT_PROVIDER=local_whisper 时需要)
WHISPER_MODEL=base
# 可选: tiny, base, small, medium, large

# ==================== LLM 语言模型配置 ====================
# 可选: openai, ollama, siliconflow
LLM_PROVIDER=openai

# OpenAI 配置
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=https://api.openai.com/v1

# Ollama 配置 (LLM_PROVIDER=ollama 时使用)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3:8b

# 硅基流动配置 (LLM_PROVIDER=siliconflow 时使用)
SILICONFLOW_API_KEY=your_siliconflow_api_key_here
SILICONFLOW_MODEL=deepseek-ai/DeepSeek-V3.2
# 其他可选模型: Qwen/Qwen3-235B-A22B, deepseek-ai/DeepSeek-R1, Qwen/Qwen2.5-72B-Instruct
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# LLM 通用配置
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4096
LLM_SYSTEM_PROMPT=你是一个有帮助的AI助手。请用简洁的语言回答问题。

# ==================== TTS 文字转语音配置 ====================
# 可选: elevenlabs, edge_tts, openai_tts
TTS_PROVIDER=edge_tts

# ElevenLabs 配置 (TTS_PROVIDER=elevenlabs 时需要)
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM
ELEVENLABS_MODEL=eleven_monolingual_v1

# Edge TTS 配置 (TTS_PROVIDER=edge_tts 时使用，免费)
EDGE_TTS_VOICE=zh-CN-XiaoxiaoNeural
# 可选中文声音: zh-CN-XiaoxiaoNeural, zh-CN-YunxiNeural, zh-CN-YunjianNeural

# 如果出现 "No audio was received"，通常是网络无法访问微软 TTS 服务导致。
# 可选：设置代理（示例：EDGE_TTS_PROXY=http://127.0.0.1:7890）。
# 运行时报错日志也会提示：未配置代理时优先检查 EDGE_TTS_PROXY。
EDGE_TTS_PROXY=

# 可选：连接/接收超时（秒）
EDGE_TTS_CONNECT_TIMEOUT=10
EDGE_TTS_RECEIVE_TIMEOUT=60

# OpenAI TTS 配置 (TTS_PROVIDER=openai_tts 时使用)
# 注意：OpenAI TTS 复用上面的 OPENAI_API_KEY（与 LLM 的 OpenAI 配置相同），无需重复填写
OPENAI_TTS_VOICE=alloy
# 可选: alloy, echo, fable, onyx, nova, shimmer
OPENAI_TTS_MODEL=tts-1

# ==================== VAD 语音活动检测配置 ====================
VAD_THRESHOLD=0.5
VAD_SILENCE_DURATION_MS=500
VAD_PREFIX_PADDING_MS=300
